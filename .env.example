NEXT_PUBLIC_API_BASE=http://localhost/keynova
API_BASE_URL=http://localhost/keynova
AUTH_SECRET=replace-with-at-least-32-random-characters

# Local agent pages use http://john-smith.localhost:3000 automatically.
# Agent subdomains use the custom apex domain in production. The base site is
# also available at https://keynova-ruby.vercel.app.
NEXT_PUBLIC_ROOT_DOMAIN=keynovagrp.com
