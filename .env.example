NEXT_PUBLIC_API_BASE=http://localhost/keynova
API_BASE_URL=http://localhost/keynova
AUTH_SECRET=replace-with-at-least-32-random-characters

# Root domain used by each agent's separate property-search site. Generated
# *.vercel.app deployments automatically use /agent/<slug> as a fallback.
NEXT_PUBLIC_ROOT_DOMAIN=keynovagrp.com
